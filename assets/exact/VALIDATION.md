# Pixel-identity validation

The approved source board is `reference/RankedUP_Approved_Asset_Sheet.png` (1448 × 1086).

Each delivery PNG was recreated in memory from the source rectangle recorded in `CROP_MAP.json` and compared against its packaged PNG with absolute-error metric `AE`.

Result: **0 differing pixels** for all nine delivery assets and both primary-logo reference crops.

No image-generation or vector-recreation step was used after selecting the approved board. The package also includes SHA-256 hashes for the board and every included crop.
